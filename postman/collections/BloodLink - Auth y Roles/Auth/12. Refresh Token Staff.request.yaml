$kind: http-request
description: Rota refresh token del Staff y genera nuevo access token.
url: "{{baseUrl}}/auth/refresh-token"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "refreshToken": "{{refreshTokenStaff}}"
    }
scripts:
  - type: afterResponse
    code: >-
      if (pm.response.code === 200) {
        const response = pm.response.json();
        if (response.accessToken) pm.environment.set('tokenStaff', response.accessToken);
        if (response.refreshToken) pm.environment.set('refreshTokenStaff', response.refreshToken);
      }
    language: text/javascript
order: 12000
