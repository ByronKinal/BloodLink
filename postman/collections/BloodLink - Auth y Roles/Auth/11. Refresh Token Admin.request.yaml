$kind: http-request
description: Rota refresh token del Admin y genera nuevo access token.
url: "{{baseUrl}}/auth/refresh-token"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "refreshToken": "{{refreshTokenAdmin}}"
    }
scripts:
  - type: afterResponse
    code: >-
      if (pm.response.code === 200) {
        const response = pm.response.json();
        if (response.accessToken) pm.environment.set('tokenAdmin', response.accessToken);
        if (response.refreshToken) pm.environment.set('refreshTokenAdmin', response.refreshToken);
      }
    language: text/javascript
order: 11000
